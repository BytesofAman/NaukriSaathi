# NaukriSaathi – Job Matching and Candidate Screening Chatbot

This is an NLP-based chatbot that evaluates a candidate’s skills, academic scores, hobbies, and soft skills, and gives a recruitment-style recommendation.

## How to Run

1. Install dependencies:
pip install -r requirements.txt



2. Run the Python script (or open notebook in Jupyter/Kaggle):

Note : Download NLTK Data

Before running the project, run this in Python:

```python
import nltk
nltk.download("punkt")
nltk.download("wordnet")
nltk.download("omw-1.4")
